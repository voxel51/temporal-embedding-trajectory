numpy
fiftyone-brain
# UMAP is the default dimensionality-reduction method; PCA/t-SNE need no extra dep
umap-learn
# Computing NEW embeddings with the built-in CLIP/DINOv2 zoo models also requires
# torch + torchvision. Omit if you only project an existing embeddings field.
# torch
# torchvision
